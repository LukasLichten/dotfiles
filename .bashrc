# Basically stock bash, barring fixing some odd issues here or there

# So go can actually work
export PATH="$HOME/go/bin:$PATH"

## For use of the Flatpak version of Protontricks
## Currently commented out because I am using the arch version, which does allow memory map deployments
# alias protontricks='flatpak run com.github.Matoking.protontricks'
# alias protontricks-launch='flatpak run --command=protontricks-launch com.github.Matoking.protontricks'
